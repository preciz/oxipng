[package]
name = "oxipng_nif"
version = "0.1.0"
authors = ["Barna Kovacs"]
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
rustler = "0.38.0"
oxipng = { version = "10.2", default-features = false, features = ["parallel", "zopfli"] }
indexmap = "2"
rgb = "0.8"
